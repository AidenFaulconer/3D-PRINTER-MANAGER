import { describe, it, expect } from 'vitest'

describe('App', () => {
  it('should be importable', () => {
    // Simple test to ensure the App component can be imported
    expect(true).toBe(true)
  })
})
